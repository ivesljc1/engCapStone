# WellPath AI - Python Requirements
# Core Web Framework
flask==2.2.5
flask-cors==4.0.0

# Firebase Integration
firebase-admin==6.3.0

# OpenAI Integration
openai==1.5.0

# Utilities
python-dotenv==1.0.0
json5==0.9.14  # Important for parsing JSON in gpt.py
pytz==2023.3   # For timezone handling in appointment module

# HTTP Requests (for tests and webhooks)
requests==2.31.0

# Environment
nodemon==0.1.1  # For development server auto-reload

# Note: The following standard library modules are also used:
# - smtplib (for email functionality)
# - uuid (for unique ID generation)
# - datetime (for date/time handling)
# - json (for JSON parsing)
# - os (for environment variables)
# - unittest (for testing)
# - email.mime.text & email.mime.multipart (for email composition) 